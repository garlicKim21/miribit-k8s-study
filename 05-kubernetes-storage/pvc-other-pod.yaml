apiVersion: v1
kind: Pod
metadata:
  name: pvc-other-pod
spec:
  containers:
    - name: myfrontend
      image: nginx
      volumeMounts:
      - mountPath: "/usr/share/nginx/html"
        name: pvc-vol
  volumes:
    - name: pvc-vol
      persistentVolumeClaim:
        claimName: test-pvc